doctype html
html(lang='en')
    include ../common/head
    +head("Leads")
    body
        header
            include ../common/navbar
            +navbar
        .container
            .row
                .col-md-6
                    form(action="/lead/"+ lead.id + "/edit", method="POST").input-group.mb-3
                        input(name="lead_email", type="email", placeholde="Enter your email address", value=lead.email).form-control
                        .input-group-append
                            button(type="submit").btn.btn-primary Submit
        include ../common/footer
        +footer()