extends layout

block content
  h1= title
  p Welcome to #{title}
  form(act="/", method="POST")
    input(name="lead_email", type="email", placeholde="Enter your email address")
    button(type="submit") Submit
